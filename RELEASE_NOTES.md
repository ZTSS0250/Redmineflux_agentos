# Release Notes — redmineflux_agentos

## [Unreleased]

### Internal
- **rao-001**: Phase 1 foundation specification completed and closed — functional spec, layered architecture, 17-agent roster, normalized database schema, MCP tool catalog, plugin navigation, permission set, agent lifecycle state machine, and ASCII UI wireframes. No application code shipped yet; the 5 open questions in `docs/PHASE1-SPECIFICATION.md` §7 still block Phase 10 (plugin skeleton).
- **rao-002**: Product Vision, Business Goals, Project Scope, Success Criteria & Assumptions completed and closed — new "Business Goals", "Project Scope", and "Assumptions & Constraints" sections added to `VISION.md`.
- **rao-003–rao-006**: Remaining Phase 1 deliverable clusters completed and closed per `ROADMAP.md` — Requirements/User Roles/User Stories (`docs/USER-ROLES-AND-STORIES.md`), AI Workflow/Multi-Agent Overview (`VISION.md` addition), MCP Vision/Security & Compliance Overview (`docs/SECURITY-COMPLIANCE-OVERVIEW.md`), and High-Level Architecture/Product Roadmap v1→v2→v3 (`docs/PRODUCT-ROADMAP.md`). All docs-only, all three quality gates approved.
- Added `ROADMAP.md` (16-phase internal build-process roadmap) and `WORKFLOW.md` (28-section end-to-end operational workflow specification with Mermaid diagrams).
- **Phase 1 status**: all six tickets (`rao-001`–`rao-006`) are now closed in `backlog/done/`; `backlog/specification/` is empty. Two requirements carried forward to future tasks: a build-time test asserting the Mock AI Provider makes zero outbound network calls (Phase 12), and a v1→v2 vendor/DPA review gate before a real LLM provider ships (`docs/PRODUCT-ROADMAP.md`).
- **rao-007**: Phase 2 (Core Technical Architecture) completed and closed — new `docs/PHASE2-CORE-TECHNICAL-ARCHITECTURE.md` adds Service-Oriented Architecture conventions, a SOLID-principles mapping, Agent Engine internals (Registry/Lifecycle/Runner + concurrency model), a Workflow Engine design, a concrete Event Bus (built on `ActiveSupport::Notifications`), Conversation/Memory/Prompt architecture, and all ten cross-cutting engineering strategies. Three requirements carried forward to future implementation tasks: Event Bus subscribers must be non-blocking, the Concurrency Guard must be atomic (not check-then-act), and the dependency-graph cache needs insert+delete invalidation test coverage.
- **rao-008**: Phase 3 (Mock AI Provider Foundation) completed and closed — new `docs/PHASE3-MOCK-AI-PROVIDER-FOUNDATION.md` defines the Provider Interface (request/response/error/capability/configuration models), the Mock AI Provider's architecture and lifecycle, Provider-specific Conversation/Agent Execution flow detail, Prompt Management and the 11-category Prompt Template Library, the Mock Response Strategy (12 deterministic scenarios), Token/Cost Simulation, and the Future Migration Plan's mechanics. A pre-close-out revision pass caught and fixed a genuine correctness bug (multiple `tool_calls` from one turn would have collided on a shared idempotency key, silently dropping records) plus three completeness gaps (missing `memory_updates` field, non-round-aware fixture selection, under-specified `latency_ms`). One requirement carried forward: real providers must require validated, non-nil credentials before activation.
- **rao-009**: Phase 4 (Database Design) completed and closed — new `docs/PHASE4-DATABASE-DESIGN.md` deepens `docs/DATABASE-SCHEMA.md` with a Mermaid ERD, a consolidated Indexing Strategy, Constraints, an Enumerations catalog, a JSON Field Usage catalog, a State Machines catalog, a deepened Audit Tables mechanism, a Soft Delete Strategy, a Versioning Strategy, and Performance Considerations. Two new decisions: no soft deletes anywhere (one status-based exception for a deleted linked issue), and "one active prompt version per key" enforced at the application layer rather than a DB constraint (MySQL doesn't portably support partial unique indexes). Three requirements carried forward: a concurrent prompt-activation race-condition test, a `to_prepare` core-association boot-mode test, and a retention job that excludes non-terminal agent runs.
- **rao-010–rao-014**: Every remaining documentation phase (5-9) completed and closed. `rao-010` (Phase 5, `docs/PHASE5-FOLDER-STRUCTURE.md`) maps every class to a file path. `rao-011` (Phase 6 expansion, `docs/PHASE6-AGENT-ARCHITECTURE.md`) completes Context/Prompt Template/Events/Retry/Escalation for all 17 agents. `rao-012` (Phase 7 deepened, `docs/PHASE7-MCP-ARCHITECTURE.md`) formalizes the Tool Registry, Permission Model, Request/Response Contracts, and Error Handling. `rao-013` (Phase 8, `docs/PHASE8-WORKFLOW-ENGINE-ORCHESTRATION.md`) resolves the previously-deferred **Pause/Resume Logic** as a scheduling gate (via the existing `configurations` table) rather than a new agent-run state, and defines explicit Completion Criteria for release readiness. `rao-014` (Phase 9 deepened, `docs/PHASE9-UI-UX-SPECIFICATION.md`) adds Information Architecture, two new pages (Prompt Library, Settings — with a mandatory credential-masking rule), and two drill-down pages (Sprint Planner, Agent Monitoring). Every ROADMAP.md documentation phase (1-9) is now fully covered.
- **rao-015–rao-021**: All seven implementation phases (10-16 — Plugin Skeleton, Database Migrations, Mock Provider Implementation, MCP Implementation, Multi-Agent Orchestration, UI Implementation, Enterprise Readiness) are now fully specified as gate-approved tickets with Code Changes tables — but **not implemented**; no application code exists. They remain in `backlog/specification/` per the Golden Rule until actually built and tested against a running Redmine instance. `rao-015` (Phase 10) is additionally blocked on the 5 open questions in `docs/PHASE1-SPECIFICATION.md` §7. All sixteen ROADMAP.md phases now have a specification; fourteen tickets (`rao-001`–`rao-014`) are closed in `backlog/done/`, seven (`rao-015`–`rao-021`) await implementation in `backlog/specification/`.
- Resolved all 5 open questions blocking Phase 10 (`docs/PHASE1-SPECIFICATION.md` §7): LLM provider (not a v1 blocker, Mock-only; Anthropic Claude as a non-binding v2 default), background job backend (plain adapter-agnostic `ActiveJob`, `redmineflux_devops` precedent), MCP transport (in-process `Mcp::Executor` plus REST exposure for the shared external "Redmineflux MCP server" — no second MCP server), confirmation UX (Pending Approvals queue on the Agent Dashboard), and code-writing-agent scope reservation (explicitly not reserved, YAGNI — deferred to a future dedicated security spec per `docs/PRODUCT-ROADMAP.md`'s v2→v3 gate). `rao-015` unblocked.
- **rao-015 implemented (untested)**: first application code in this plugin — 123 files covering `init.rb`, routes, locale, initializer, 18 models, 16 controllers with placeholder views, 17 agent classes, Provider/MCP/Engine/Services `lib/` namespaces, job skeletons, asset stubs, and a real AgentOS System user provisioner (non-admin, non-interactive-login, project `Member` with a scoped Role — the `User.current` identity for autonomous agent actions). Status stays `specification`, not `done` — untested against a live Redmine instance. Corrected a model-path naming inconsistency found in `docs/PHASE5-FOLDER-STRUCTURE.md` §2 while implementing (flat `RedminefluxAgentos*` class names, per CLAUDE.md, not a namespaced subdirectory).
- **rao-016 implemented (untested)**: 18 `db/migrate/` migrations giving Phase 10's models their first real backing schema — one `create_table` per table in `docs/DATABASE-SCHEMA.md`, every index from `docs/PHASE4-DATABASE-DESIGN.md` §4, every FK declared at the DB level per §5 (deliberately no partial unique index for `prompt_templates.is_active` — MySQL portability). Status stays `specification`, not `done` — untested against a live Redmine instance. Two correctness fixes made during implementation, not design changes: FK columns referencing a `bigint`-PK table use `t.bigint`, not `t.integer` (PostgreSQL rejects a type-mismatched `add_foreign_key`); every composite/unique index got an explicit short name to stay under MySQL's 64-character identifier limit (default Rails-generated names came within 1 character of it on this plugin's longer table names — the exact risk `rao-016`'s own Gate 3 review had predicted). Also fixed a genuine FK-ordering gap in the ticket's own table list (`agent_runs` referenced `conversations` before that table existed in the listed order) by creating `conversations` before `agent_runs` on disk — logged as a transparent Gate 1 revision pass. Phase 10 + Phase 11 together give the plugin a bootable schema for the first time.
- **rao-017 implemented (untested)**: the Provider Interface, `Provider::Registry.active`, and the full Mock Provider pipeline (Fixture Selector → Loader → Renderer → Standard Response) — deterministic, fixture-based, zero outbound network calls. 15 fixture files cover all 12 `docs/PHASE3-MOCK-AI-PROVIDER-FOUNDATION.md` §7 scenarios plus a generic fallback fixture; a new `TicketGenerationRule` implements §7.2's deterministic story/task/point-generation algorithm (not hand-authored, since epic × story × task combinatorics make that impractical). A Minitest suite covers the mandatory zero-network-call assertion, determinism, multi-tool-call idempotency-key suffixing, boot-time fixture-directory validation, and round-qualified fixture selection — every logic path was also smoke-tested standalone outside Rails against the real fixture files first, and the actual test files were then run (unmodified) through the real Minitest+Mocha runner against a minimal ad hoc harness: **10/10 passed**, after fixing one genuine bug the first run caught in the test file itself (a missing `agent_key` override that was silently resolving both Clarification Questions rounds to the fallback fixture). Status stays `specification`, not `done` — this environment has no live Redmine instance, real database, or Redmine core, so that layer of verification is still the developer's. Two cross-cutting gaps were found and filled, logged as a transparent Gate 1 revision: `RedminefluxAgentos::Error` (Phase 2 §B.7) and a minimal `Configuration::Store.get` read path (Phase 2 §B.6) were both referenced throughout the approved docs and required by this ticket's own Provider Lifecycle/Error Model, but neither was ever itemized as a file in any of the seven implementation tickets' Code Changes tables — both were added, scoped tightly to only what this ticket needs.
- **rao-018 implemented (untested)**: `Mcp::ToolRegistry`, `Mcp::Executor` (`call`/`confirm`/`reject`), and all 20 tools across the six category files — the plugin's entire write path to real Redmine data (projects, issues, wiki pages, files, time entries, reports), all funneled through one Permission Model (Redmine authorization + agent tool-allowlist), a confirmation gate for destructive actions (`bulk_close_issues`, `delete_issue`, `update_timesheet`), idempotency-key deduplication, and allow-list-based secrets redaction. `Mcp::Executor`'s own generic mechanics were run — not just written — through the real Minitest+Mocha runner against a synthetic registered tool: **15/15 pass**, catching two real bugs first: `Mcp::ToolRegistry.register` never actually accepted the `authorize:` keyword every tool file passes it (would have crashed plugin boot entirely), and a fresh execution's result was symbol-keyed while a deduped replay's was string-keyed (now both normalized to the same shape). Status stays `specification`, not `done` — real Issue/Project/WikiPage/TimeEntry/Attachment creation via the six tool files still needs a live Redmine instance to verify. Several interpretive gaps neither `docs/MCP-TOOLS.md` nor `docs/PHASE7-MCP-ARCHITECTURE.md` fully specifies were filled and logged transparently: a per-tool `authorize:` proc for Layer 1 (keeps `Mcp::Executor` tool-agnostic), an added `agent:` keyword on `Mcp::Executor.call` for Layer 2, a new additive migration for the `idempotency_key` column `docs/DATABASE-SCHEMA.md` never included, and an explicit per-item-reporting (not all-or-nothing-transaction) decision for the two batch tools.
- **rao-019 implemented (untested)**: `AgentEngine::{Registry,Lifecycle,Runner}`, `WorkflowEngine::StateMachine` (agent-run and ticket-status configured instances), `EventBus`, `DependencyEngine::{Graph,Scheduler}`, `ConcurrencyGuard`, `MemoryStore::Repository`, `Prompts::TemplateResolver`, and all 17 agent classes — the orchestration layer wiring every prior phase together. 39 tests (147 assertions) actually ran against a scoped ad hoc Minitest+Mocha harness, including the Gate-3-required contract-conformance suite run against all 17 agent classes at once: **0 failures, 0 errors**. Status stays `specification`, not `done` — `Runner`'s full Provider+MCP integration and the literal concurrent-thread concurrency race need a live Redmine instance/real database to verify. The most consequential bug this pass caught: `issue_tools.rb`'s (`rao-018`) status-changing handlers never published an event, but `DependencyEngine::Scheduler` (this ticket) subscribes to `issue.status_changed` to auto-resume blocked agent runs — without the fix, dependency-driven blocking/resuming (this ticket's own headline feature) would never have fired on a live instance. Also fixed: the AgentOS System user's role (`rao-015`) never had the actual Redmine core permissions MCP tools require, so no agent-initiated write could have passed Layer 1 either. Three more modules referenced throughout the approved docs but never itemized in any Code Changes table were added, scoped tightly: `ConcurrencyGuard`, `MemoryStore::Repository`, and `Prompts::TemplateResolver`'s real implementation — plus two idempotent seed rake tasks (`seed_agents`, `seed_prompt_templates`), since no prior ticket ever seeded the rows a fresh install needs before any agent could run. One acknowledged, not-closed gap: the per-project dependency-graph cache (a performance layer, not a correctness one) was not implemented.
- **rao-020 implemented (untested)**: all 13 UI pages across 9 controllers, every dashboard reading its denormalized data source directly (`docs/PHASE9-UI-UX-SPECIFICATION.md` §6), and a new `Configuration::CredentialMasking` module implementing Gate 2's mandatory secret-masking mechanism. The Agent Dashboard's Pending Approvals flow is a real, working integration — `approve`/`reject` call straight into the already-tested `Mcp::Executor.confirm`/`.reject` (`rao-018`), not a stub. `Admin::PromptTemplatesController` implements real version-activation logic (deactivate-then-activate in one transaction, Phase 4 §11). Status stays `specification`, not `done` — this is a UI phase, so most of it needs a live Redmine instance's real browser/routing/view-rendering stack to functionally verify; `CredentialMasking`'s pure logic (the one genuinely browser-free-testable piece) was run through the usual ad hoc-harness approach: **6/6 pass**, catching one real bug first — a bare `/token/i` sensitive-key pattern false-positived against the legitimate v1 config key `token_rules`. One acknowledged gap, not silently worked around: `ChatController`/`RequirementReviewsController` correctly persist their own state but don't trigger the agent turn that should follow — that bridging is `ConversationManager::Session`'s job (Phase 2 §A.8), which no ticket has ever built or been assigned to build.
- **rao-021 implemented (untested)** — the final phase, a verification/hardening pass rather than new features. **All four of this ticket's own named carried-forward requirements are now confirmed**: `rao-007`'s dependency-graph/prompt-template/agent-registry caches are `Rails.cache`-backed with explicit invalidation on both insert **and** delete (delete required a new `DependencyEngine::Graph.remove_edge` — nothing removed an edge before this ticket); `rao-009`'s `LogRetentionJob` is implemented and excludes non-terminal `agent_run`s regardless of age; `rao-011`'s reserved `:code_review` scheduling guard and `rao-013`'s Concurrency Guard atomicity were both already implemented in `rao-019` (Phase 14) and are reconfirmed unchanged here. Also implemented: `NotificationCenter` (4 of `WORKFLOW.md` §23's 6 rows, through a new plugin mailer) and an unauthenticated `HealthController` (boot-state + ops metrics — a deliberate, documented exception to the blanket `require_login` rule, since infrastructure probes cannot authenticate as a Redmine user). This ticket's own RBAC audit found and fixed a real bug: `admin/audit_logs` had zero permission mapping anywhere in `init.rb`, making it unreachable by any user, including admins, because Redmine's `authorize_global` fails closed on an undeclared controller/action pair. It also found and fixed three related gaps that would have silently undermined this ticket's own new features: `Mcp::Executor.call` never populated `mcp_tool_calls.agent_run_id` despite the column/association existing since `rao-016`/`rao-018` (fixed with an additive, backward-compatible `agent_run:` keyword); nothing anywhere published `agent_run.running` (fixed in `ConcurrencyGuard.acquire`); and nothing anywhere read `RedminefluxAgentosAgent#status` before scheduling a run, so disabling an agent would have had no effect on its queued work (fixed via a new `AgentEngine::Registry.enabled?` cache plus a `Lifecycle` guard). Status stays `specification`, not `done` — same reason as every prior phase: no live Redmine instance in this environment. 61 ad hoc Minitest+Mocha-harness test runs (mirroring the real, now-committed `test/unit/**` files), **60 pass**; the one non-pass needs the full plugin boot sequence (17 agent classes, 6 MCP tool files, Mock Provider fixtures) to replicate and was instead verified correct by reading `config/initializers/redmineflux_agentos.rb` directly. Two gaps explicitly not implemented, logged transparently rather than worked around: `Admin::AgentsController`/`Admin::McpToolsController` are still Phase 10 skeleton stubs with no assigned ticket to build their real CRUD, and 2 of `WORKFLOW.md` §23's 6 notification rows ("Workflow Blocked... SLA at risk", "Project Completed") have no real trigger/detection mechanism anywhere and would need new feature design (SLA tracking, completion detection) this ticket's "verification pass, not new design" scope excludes. **All 16 ROADMAP.md phases now have both a specification and implementation code** — the plugin is feature-complete pending live-instance verification and an actual release.
