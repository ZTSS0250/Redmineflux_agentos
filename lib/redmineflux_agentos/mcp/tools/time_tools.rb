# frozen_string_literal: true

module RedminefluxAgentos
  module Mcp
    module Tools
      # create_time_entry, update_timesheet (requires_confirmation),
      # update_workload (docs/MCP-TOOLS.md "Time & workload"). Phase 13
      # (rao-018) implements each handler and registers it into Mcp::ToolRegistry.
      module TimeTools
      end
    end
  end
end
