# docs/PHASE3-MOCK-AI-PROVIDER-FOUNDATION.md §7 — Agent Assignment
# scenario: tool_calls only (assign_issue).
tool_calls:
  - tool_name: redmineflux_agentos_assign_issue
    params:
      issue_id: "{{issue_id}}"
      assignee_key: "{{assignee_agent_key}}"
usage:
  prompt_tokens: 180
  completion_tokens: 60
latency_ms: 250
finish_reason: tool_calls
